---
import Footer from "@components/Footer.astro";
import Header from "@components/Header.astro";
import Layout from "@layouts/Layout.astro";
---

<Layout title="404 - Página no encontrada">
    <Header />
    <main>
        <h1>404</h1>
        <div>
            <h2>Página no encontrada</h2>
            <p>Lo sentimos, la página que buscas no existe.</p>
        </div>
        <a href="/">Volver al inicio</a>
    </main>
    <Footer />
</Layout>

<style>
    main {
        position: relative;
        flex: 1;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: 2rem;
        padding: 1rem;
        overflow: hidden;
    }

    main > h1 {
        position: absolute;
        transform: rotate(-25deg);
        font-size: clamp(4rem, 50vw + 1rem, 50rem);
        color: light-dark(var(--neutral-200), var(--neutral-800));
        z-index: -1;
    }

    main > div {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 0.5rem;
        text-align: center;
    }

    main > div > h2 {
        font-size: var(--text-size-large);
    }

    main > a {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 0.5rem;
        padding: 16px 32px;
        background-color: var(--primary-500);
        color: var(--neutral-50);
        text-align: center;
        font-weight: 600;
        border-radius: var(--border-radius-out);
        transition:
            opacity 0.3s ease-in-out,
            background-color 2s ease-in-out;

        &:hover {
            opacity: 0.8;
        }

        &:disabled {
            cursor: not-allowed;
            opacity: 0.6;
        }
    }
</style>
